import { ObjToKeysPipe } from './obj-to-keys.pipe';

describe('ObjToKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjToKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
